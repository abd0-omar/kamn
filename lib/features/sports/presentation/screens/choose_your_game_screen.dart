import 'package:flutter/material.dart';

class ChooseYourGameScreen extends StatelessWidget {
  const ChooseYourGameScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('Choose Your Game')),
    );
  }
}
